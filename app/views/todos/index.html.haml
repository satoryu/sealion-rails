%h1 Todos

= button_to 'Sign in with GitHub', user_github_omniauth_authorize_path, data: { turbo: false }

= turbo_frame_tag 'todo-form', target: '_top' do
  = render 'form', todo: @todo

= turbo_frame_tag 'todo-list' do
  = link_to 'Completed Todos', completed_todos_path

  %ul
    - @todos.each do |todo|
      = turbo_frame_tag dom_id(todo) do
        %li
          = todo.content
          = link_to 'Complete', complete_todo_path(todo), data: { turbo_method: :post }
